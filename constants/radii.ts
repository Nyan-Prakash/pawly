/**
 * Three radii, each with a meaning. See DESIGN.md.
 *   sm   controls: inputs, chips, tags, small buttons
 *   md   containers: grouped lists, cards, primary buttons, sheets
 *   full avatars, progress bars, dots
 */
export const radii = {
  sm: 8,
  md: 12,
  full: 999,
} as const;
